services:
  replaykit:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      REPLAYKIT_STORAGE: sqlite
      REPLAYKIT_DATA_ROOT: /data
    volumes:
      - replaykit-data:/data
    command: ["demo-branch"]

volumes:
  replaykit-data:
