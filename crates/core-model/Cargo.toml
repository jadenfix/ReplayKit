[package]
name = "replaykit-core-model"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
name = "replaykit_core_model"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
